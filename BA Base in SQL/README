This folder contains an implementation of a data pipeline for psychological research data, rewritten from Pandas to SQL (DuckDB). The goal was to transform raw data collected during the study into clean, analysis-ready tables used for further statistical analysis.

Originally, the data was processed using Python and Pandas. In this project, all key processing steps have been reimplemented in SQL using the DuckDB engine.
